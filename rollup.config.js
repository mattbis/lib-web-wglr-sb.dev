//const resolve = require('@rollup/plugin-node-resolve')
//const terser = require('rollup-plugin-terser').terser
/*module.exports = {
    input: 'src/index.js',
    exclude: ['src/package.json','src/package-lock.json'],
    output: {
        //file: '_dist/index.js',
        format: 'cjs',
        sourcemap: false,
        // globals:{
        //  jquery: '$'
        // }
    },
    plugins: [
        //nodeResolve(),
        //commonjs(),
        //json(),
        terser({
            output: {
                comments: false
            }
        })
    ]
}
*/
